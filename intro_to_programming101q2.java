public class intro_to_programming101q2<def> {
    public static double sumIf(double arr, double f){
        return (x +y);
    }

    public static double maxArray(double[] arr) {
        double maxVal = arr[0];  
        for (int i = 1; i < arr.length; i++) {
            maxVal = max(maxVal, arr[i]);  
        }
        return maxVal; 
    }
    public static void main(String[] arg) {
        double[] numbers = {1, 2, 3, 4, 5}; //ENTER NUMBERS HERE
        System.out.println("The sum is: " + maxArray(numbers));
    }
}
